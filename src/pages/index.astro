---
import BaseLayout from "../layouts/BaseLayout.astro";
import HorizontalCard from "../components/HorizontalCard.astro";
import { getCollection } from "astro:content";

const posts = (await getCollection("blog")).sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf());

const last_posts = posts.slice(0, 3);
---

<BaseLayout sideBarActiveItemID="home">
  <div class="pb-12 mt-5">
    <div class="text-xl py-1">Olá 👋</div>
    <div class="text-5xl font-bold">Eu sou Guilherme Zobel</div>
    <div class="text-3xl py-3 font-bold">Desenvolvedor Front-end</div>
    <div class="py-2">
      <text class="text-lg">
        Programar é mais do que uma simples tarefa, é uma jornada de constante aprendizado e crescimento.
        São evoluções que requerem resiliência, colaboração, compartilhamento, inovação, criatividade.
        Cada desafio é uma oportnidade de evoluir como profissional e indivíduo.
      </text>
    </div>
    <div class="mt-8">
      <a class="btn" href="https://api.whatsapp.com/send?phone=5551998054321&text=Guilherme%20Zobel" target="_blank"> Vamos nos conectar!</a>
    </div>
  </div>

  <div>
    <div class="text-3xl w-full font-bold mb-2">Meus últimos projetos {"</>"}</div>
  </div>

  <HorizontalCard
    title="Ignews"
    img="/project_ignews.png"
    desc="O projeto ig.news é um blog onde os usuários podem ter acesso ao conteúdo de cada postagem de acordo com o status de sua assinatura."
    url="https://github.com/guilhermezobel/project-ignews"
    badge=""
  />
  <div class="divider my-0"></div>
  <HorizontalCard
    title="Cadastro de Usuários"
    img="/project_cadastro_de_usuarios.png"
    desc="O projeto é um CRUD unindo back-end com front-end."
    url="https://github.com/guilhermezobel/projeto-cadastro-de-usuario"
  />
  <div class="divider my-0"></div>
  <HorizontalCard
    title="Dt Moneey"
    img="/project_dtmoney.png"
    desc="DT Money é um controle de contas financeiro pessoal que cadastra contas de entrada e saída, totaliza em cards utilizando uma biblioteca de Fake API MirajeJS que utiliza o formato JSON para fazer os cadastros."
    url="https://github.com/guilhermezobel/project-dtmoney"
    badge=""
  />

  <!-- <div>
    <div class="text-3xl w-full font-bold mb-5 mt-10">Latest from blog</div>
  </div>

  {
    last_posts.map((post) => (
      <>
        <HorizontalCard
          title={post.data.title}
          img={post.data.heroImage}
          desc={post.data.description}
          url={"/blog/" + post.slug}
          target="_self"
          badge={post.data.badge}
        />
        <div class="divider my-0" />
      </>
    ))
  } -->
</BaseLayout>
